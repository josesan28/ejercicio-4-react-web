# docker-compose.yml
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5174:5173"
    volumes:
      - ./ejercicio-4-web:/app
      - /app/node_modules
    env_file:
      - ./ejercicio-4-web/.env